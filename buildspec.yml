version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Build started...
      - echo No compilation needed for sample app

  post_build:
    commands:
      - echo Build completed successfully

artifacts:
  files:
    - '**/*'
